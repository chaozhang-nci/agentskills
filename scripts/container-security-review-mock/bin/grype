#!/usr/bin/env bash
set -euo pipefail
source "$(cd "$(dirname "$0")" && pwd)/_common.sh"

if [[ "${1:-}" == "version" ]]; then
  echo "Application:         grype"
  echo "Version:             0.82.0"
  exit 0
fi

cat "$(fixture_file grype)"
exit 0
